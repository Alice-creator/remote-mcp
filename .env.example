# --- Gateway ---
GATEWAY_PORT=8000

# --- LLM (OpenAI-compatible /chat/completions endpoint) ---
# Ollama Cloud / Turbo:
PLEXUS_BASE_URL=https://ollama.com/v1
PLEXUS_API_KEY=
PLEXUS_MODEL=gpt-oss:20b

# Optional system prompt the gateway prepends to chat conversations.
# PLEXUS_SYSTEM_PROMPT="You are a smart-home assistant..."

# --- Legacy worker config (no longer used; kept for reference) ---
# MCP_PORT=8001
# CLAUDE_WORKING_DIR=~/Projects
# FACTORY_SECRET=change-me
# WORKER_NAME=my-pc
